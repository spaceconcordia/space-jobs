#include "gtest/gtest.h"
#include <timer.h>

class Job_Runner_Test : public ::testing::Test
{
    protected:
    virtual void SetUp() { }
};

// Test writing to the helium device
TEST_F(Job_Runner_Test, Boilerplate)
{
    ASSERT_EQ(
        0, // expected
        0 // actual
    );
}

// Test writing to the helium device
TEST_F(Job_Runner_Test, Boilerplate)
{

    ASSERT_EQ(
        0, // expected
        0 // actual
    );
}
